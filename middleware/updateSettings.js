export default function ({store, redirect, route, $axios}) {
  //const settingsMethod = '/settings'
  //$axios.$get(settingsMethod).then((resp) => {
  //  store.commit('settings/setAndProcessData', resp.data || []);
  //})
}
